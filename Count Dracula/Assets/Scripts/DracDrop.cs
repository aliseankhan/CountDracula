using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DracDrop : MonoBehaviour
{
    //public List<GameObject> fakeFloorTiles;

    //public bool isAbove;
    //bool dropped = false;
    //Vector3 startPos;
    //Vector3 endPos;

    // Start is called before the first frame update
    void Start()
    {
        //startPos = this.gameObject.transform.position;
        //endPos = new Vector3(transform.position.x, transform.position.y - 30, transform.position.z);
    }

    // Update is called once per frame
    void Update()
    {
        //if (
        //    (dropped == false)
        //    & (drac.transform.position.x == this.gameObject.transform.position.x - 1)
        //    //& drac.transform.position.y > this.gameObject.transform.position.y
        //    & (drac.transform.position.z == this.gameObject.transform.position.z + 1)
        //    )
        //{
        //    isAbove = true;
        //    this.gameObject.transform.position = Vector3.Lerp(startPos, endPos, 1);
        //    dropped = true;
        //}



    }
}
